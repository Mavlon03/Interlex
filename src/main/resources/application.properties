# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/interlex_db
spring.datasource.username=postgres
spring.datasource.password=root123
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.auto-commit=false

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Application Configuration
spring.application.name=Interlex
server.port=8080
spring.security.require-ssl=false
server.ssl.enabled=false

# Import sensitive/security properties (contains jwt.secret etc.)
spring.config.import=classpath:application-security.properties

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mamatmurodovfirdavs@gmail.com
spring.mail.password=azfezseqjdnodfwt 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# Faqat kerak bo?lsa
spring.web.cors.allowed-origins=http://localhost:63342
spring.web.cors.allowed-methods=GET,POST

# Custom Application Properties
app.admin.email=mamatmurodovfirdavs@gmail.com
app.contact.auto-assign=true
app.notification.enabled=true

# Logging Configuration
logging.level.uz.pdp.interlex=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Jackson Configuration
spring.jackson.time-zone=Asia/Tashkent
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
# Admin panel konfiguratsiyasi
app.admin.dashboard-refresh-interval=10000 # 10 soniya
app.stats.success-threshold=70.0 # Muvaffaqiyatli ishlar foizi

